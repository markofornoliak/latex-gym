import { useAppStore } from '../store/useAppStore';
function bucket(at:string){const d=new Date(at);const now=new Date();const one=86400000;const diff=Math.floor((new Date(now.getFullYear(),now.getMonth(),now.getDate()).getTime()-new Date(d.getFullYear(),d.getMonth(),d.getDate()).getTime())/one);return diff===0?'Сегодня':diff===1?'Вчера':diff<7?'На этой неделе':'Ранее';}
export function HistoryPage(){const history=useAppStore(s=>s.history);const groups=['Сегодня','Вчера','На этой неделе','Ранее'];return <div className="page editorial-page"><header className="page-intro"><span className="eyebrow">ИСТОРИЯ</span><h1>История обучения</h1><p>Краткая хронология выполненной работы без социальных механик и лишней статистики.</p></header>{!history.length&&<div className="empty-state">История появится после завершения уроков, задач и изучения команд.</div>}{groups.map(g=>{const rows=history.filter(h=>bucket(h.at)===g);return rows.length?<section className="history-group" key={g}><h2>{g}</h2>{rows.map(h=><div className="history-row" key={h.id}><span>{new Date(h.at).toLocaleTimeString('ru',{hour:'2-digit',minute:'2-digit'})}</span><p>{h.text}</p></div>)}</section>:null})}</div>}
