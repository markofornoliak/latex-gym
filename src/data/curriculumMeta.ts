// Small bootstrap-safe metadata. The companion test locks these values to the
// canonical curriculum so the shell never needs to import the full data graph.
export const CURRICULUM_LESSON_COUNT=68;
export const CURRICULUM_EXERCISE_COUNT=204;
export const CURRICULUM_MODULE_COUNT=15;
export const CURRICULUM_PROJECT_COUNT=5;
